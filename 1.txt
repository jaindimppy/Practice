Using versioning

Database: Add a version column (integer).

Initial State: Balance = $100, Version = 1.

Step 1: User A reads the row (Balance: $100, Version: 1).

Step 2: User B reads the row (Balance: $100, Version: 1).

Step 3: User A calculates $150 and sends: UPDATE account SET balance=150, version=2 WHERE id=1 AND version=1.



Result: Success! The database sees version 1 matches, updates the row, and increments it to 2.

Step 4: User B calculates $130 and sends: UPDATE account SET balance=130, version=2 WHERE id=1 AND version=1.



Result: Fail! The database sees that the version is now 2, not 1. The WHERE clause fails.

Next Step: Your API should catch this failure, re-read the new balance ($150), ask the user if they still want to add $30, or automatically retry the calculation.


1)Two APIs updating same row, how to handle?
Using
Lock
Step 1: User A initiates a transaction: SELECT * FROM account WHERE id=1 FOR UPDATE.
Database action: The row is now locked.
Step 2: User B initiates the same command: SELECT * FROM account WHERE id=1 FOR UPDATE.
Database action: The database tells User B to wait (block) until User A finishes.
Step 3: User A performs UPDATE account SET balance=150.
Step 4: User A commits the transaction.
Database action: The lock is released.
Step 5: User B's pending command now proceeds. It reads the new balance ($150).
Step 6: User B performs UPDATE account SET balance=180 and commits.
User experiences a slight delay while waiting for the lock to release. use lock

2)Difference between Encapsulation and Abstraction
1. Encapsulation (Protecting the "Inside")
You are 100% correct: Encapsulation is about access control. By making variables private, you prevent the "outside world" from breaking your object's internal state.
Key Tool: Access Modifiers (private, protected).
The "Hiding" Goal: To prevent corruption (e.g., someone setting age = -5 or balance = -999999).
The "Getter/Setter" Bridge: These aren't just for reading/writing; they act as Gatekeepers. They are where you put your validation logic (if (age > 0) { ... }).

2. Abstraction (Simplifying the "Outside")
Abstraction is about hiding implementation complexity.
Key Tool: Interfaces, Abstract Classes, and Public Methods.
The "Hiding" Goal: To prevent cognitive overload. If I am using your Bank class, I don't want to see the 50 lines of code you wrote to verify a user's identity, calculate interest, and update a database. I just want to see the transfer() method.


3)CTE and Temp table
CTE-(Common Table Expression)
A temporary named result set defined using WITH clause.
WITH DuplicateEmail AS
(
    SELECT Id,
           ROW_NUMBER() OVER
           (
               PARTITION BY Email
               ORDER BY Id
           ) AS RowNum
    FROM Employee
)
DELETE FROM DuplicateEmail
WHERE RowNum > 1
cte:No physical storage
No indexing
Temp Table:
Stored in TempDB
Can create index


CREATE TABLE #Employee
(
    Id INT,
    Name VARCHAR(100)
)

INSERT INTO #Employee
SELECT Id, Name FROM Employee

SELECT * FROM #Employee


A CTE contains:
✅ Both rows AND columns (like a table)


3)Optimization
Avoid "Select *"
Solve the "N+1" Problem
 Use Async/Await Always use the Async versions of database calls (e.g., ToListAsync(), ExecuteNonQueryAsync()). 
SQL Profiler
Indexing
Eager Loading

4)How index work
If you create an index on the Email column, SQL Server takes all those email addresses and puts them in order.
Without an Index: The database sees: zack@mail.com, adam@mail.com, ben@mail.com. To find ben@mail.com, it might have to check every row because they are in no particular order.
With an Index: SQL stores them as: adam@mail.com -> ben@mail.com -> zack@mail.com.

5)
1)Restrict or validate the file size 
2)This is the most important fix. Instead of reading the whole file into a byte[] or a MemoryStream, you should stream it directly from the request body to the target (like Azure Blob Storage or a local disk).
3)Use file stream not memory stream
Entire file loaded into RAM first
Suppose user uploads:
movie.mp4 = 2 GB

Flow:
Client
   ↓
Server RAM Memory
   ↓
Application Processing

Entire 2 GB file goes into RAMProblem
Suppose:
Server RAM = 4 GB
3 users upload 2 GB files simultaneously
Memory needed:
2 GB × 3 = 6 GB

[HttpPost]
public async Task<IActionResult> Upload(IFormFile file)
{
    var ms = new MemoryStream();

    await file.CopyToAsync(ms);

    return Ok();
}
Client uploads file
        ↓
Server receives request
        ↓
Application copies file into RAM (MemoryStream)
        ↓
Then application may save to hard disk/database/cloud



Streaming
File processed little by little (chunks)
Client
   ↓
Small chunk arrives
   ↓
Immediately written to disk
   ↓
Next chunk arrives
   ↓
Written to disk

Chunk Example

Suppose:

File = 2 GB

Streaming may process:

Chunk 1 = 64 KB
Chunk 2 = 64 KB
Chunk 3 = 64 KB

Only small chunk stays in memory at a time.
[HttpPost]
public async Task<IActionResult> Upload(IFormFile file)
{
    var path = Path.Combine("Uploads", file.FileName);

    using var fs = new FileStream(path, FileMode.Create);

    await file.CopyToAsync(fs);

    return Ok();
}
Client
   ↓
Small chunk enters RAM buffer
   ↓
Immediately written to hard disk
   ↓
Next chunk
   ↓
Hard disk


6)Multiple Inheritance
which occurs when a class inherits from two classes that both have a method with the same name, leading to ambiguity about which method the child class should use.
Imagine you have two interfaces, IWork and IExercise, and both have a method called Start().
public interface IWork
{
    void Start();
}
public interface IExercise
{
    void Start();
}
If a class implements both, C# doesn't know which Start() is which. To solve this, you prefix the method name with the Interface name.
public class Manager : IWork, IExercise
{
    // This handles IWork.Start()
    void IWork.Start() 
    {Console.WriteLine("Opening Laptop and starting work...");
    }
    // This handles IExercise.Start()
    void IExercise.Start() 
    {
        Console.WriteLine("Hitting the gym and starting exercise...");
    }
}

How to call them
Because the methods are "explicit," they are hidden from the class's normal public view. You must cast the object to the specific interface you want to use.
Manager myManager = new Manager();
// myManager.Start(); // This would cause a COMPILE ERROR (Ambiguous)

// Correct Way:
IWork worker = myManager;
worker.Start(); // Outputs: Opening Laptop and starting work...

IExercise athlete = myManager;
athlete.Start(); // Outputs: Hitting the gym and starting exercise...

7)
Protected
Scope: Accessible within the same class AND by derived (child) classes.
When to use: When you want to hide data from the outside world but allow "children" to use or override that logic.
public class Animal
{
    protected string HeartbeatType = "Standard"; 
}

public class Dog : Animal
{
    public void CheckHeart()
    {
        Console.WriteLine(HeartbeatType); // Legal: Dog is a child of Animal
    }
}

// Outside:
// var a = new Animal();
// a.HeartbeatType; // ERROR: Not accessible from outside
but you will use like 
a.CheckHeart()
it will allow


LSP:
public class PaymentProcessor {

    public virtual void Refund(double amount) {

        Console.WriteLine($"Refunding {amount}...");

    }

}

public class CashPayment : PaymentProcessor {

    public override void Refund(double amount) {

        // LSP VIOLATION: The program expects a refund to happen,

        // but this class crashes the app instead.

        throw new NotSupportedException("Cash cannot be refunded via system!");

    }

}

parent class is there and child class called the method but violets it function then LSP violation so in this on't use inheritance. Use a simple interface or keep them as completely separate classes.
in ISP:Why did you force me to add a Refund method to my CashPayment class when cash doesn't work that way?"
means dont makee the all method in one interface so anyone not have to forcely use them.

LSP Violation: You inherit from a class but have to "change the rules" (like throwing exceptions or making properties behave weirdly) to make it work.
LSP Solution: If two things aren't truly swappable, don't use inheritance. Use a simple interface or keep them as completely separate classes.

ACID
Atomicity 
It ensures that a transaction is treated as a single, indivisible unit. Either the entire transaction succeeds, or the entire transaction fails. There is no "half-finished" state.
Consistency
 If a database column has a rule that "Balance cannot be negative," and a transaction tries to withdraw more money than is available, the database will reject the transaction to maintain Consistency

Isolation 
Each transaction should feel like it is the only one running.
If two people try to buy the last concert ticket at the exact same millisecond, Isolation ensures the system processes one first and tells the second person the ticket is sold out, rather than selling the same ticket twice.

Durability 
Durability guarantees that once a transaction has been committed, it will remain committed even in the case of a system failure 


SQL Server Profiler is a powerful interface for creating and managing traces and analyzing trace results. It allows you to see exactly what queries are being sent to your database in real-time, which is incredibly helpful for debugging .NET applications or finding slow-running stored procedures.Go to the Tools menu at the top.

Select SQL Server Profiler.

RPC:Completed (Remote Procedure Call): This tracks Stored Procedures. When your C# code calls a procedure, it appears here. It shows you the name of the SP and the values of the parameters passed in.

SQL:BatchCompleted: This tracks Ad-hoc Queries. If you are using Entity Framework (EF) or raw SQL strings like SELECT * FROM Users, they will appear here.
Estimated Plan: Press Ctrl + L. This shows you the plan without running the query.

Actual Plan: Press Ctrl + M (or click the "Include Actual Execution Plan" icon) and then Execute your query. A new tab called "Execution Plan" will appear in the results window.

